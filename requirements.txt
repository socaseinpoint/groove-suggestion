fastapi>=0.100.0
uvicorn[standard]>=0.20.0
pydantic>=2.0.0
mido>=1.2.10
numpy>=1.20.0
# pretty_midi # Removed - Using pydub instead
scipy # pydub might use this, keep for now
pydub
# Add dependencies from midi_to_wav.py if known, e.g.:
# pydub>=0.25.0
# soundfile>=0.12.0
